import { StyleSheet } from 'react-native';
import { Colors } from '../../Themes/Colors';

const Styles = StyleSheet.create({
  headerTitle: {
    color: Colors.white,
    fontSize: 20,
  },
});

export { Styles };
